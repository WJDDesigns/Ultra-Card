/**
 * Lazy half of the Ultra Dashboard strategy (`strategy` chunk): the generator
 * and the config editor element. `ultra-dashboard-strategy.ts` in the entry
 * imports this on first use.
 */
import './ultra-dashboard-strategy-editor';

export { generateUltraDashboard, generateUltraAreaView } from './uc-dashboard-generator';
