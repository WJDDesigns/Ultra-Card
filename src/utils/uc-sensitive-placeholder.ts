/**
 * Placeholder written into exports where a sensitive value (API key, secret)
 * was redacted. Lives in its own file so runtime code (e.g. the map module)
 * can recognise it without importing the export/import machinery and pako.
 */
export const SENSITIVE_PLACEHOLDER = '***REDACTED***';
