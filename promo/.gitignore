node_modules/
out/
# Alternate tracks stay local. public/music/teaser-music.mp3 is tracked (force-added, see public/music/LICENSE.md).
# Clips in public/clips/ and renders in out/ are ignored too (root .gitignore ignores *.mp4); `git add -f` when needed.
public/music/*.mp3
public/music/*.wav
public/music/*.m4a
